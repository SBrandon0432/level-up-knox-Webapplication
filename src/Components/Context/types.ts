export interface StoreImages {
    image: string;
    name: string;
}

export interface StoreCard {
    icon: string;
    location: string;
    locationHerf: string;
    calender: string[];
}

export interface EmployeeCard {
    image: string;
    hobby: string;
    position: string;
    about: string;
}
