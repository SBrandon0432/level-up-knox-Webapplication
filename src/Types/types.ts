export enum Paths {
    DEFAULT = "/",
    HOME = "/home",
    MEETTEAM = "/meetteam",
    CALENDER = "/calender",
}

export enum isOpen {
    OPEN = "success",
    CLOSINGSOON = "warning",
    CLOSED = "danger",
}
